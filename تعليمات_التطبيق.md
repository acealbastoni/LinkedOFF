# 🔒 دليل التطبيق - نظام الحماية JobHub KSA

## ✅ ما تم إضافته:

تم إنشاء **ملفين جديدين فقط**:
1. **`login.html`** - صفحة تسجيل الدخول
2. **`js/auth.js`** - نظام الحماية

## 📝 الخطوات (3 خطوات بسيطة):

### الخطوة 1: أضف الملفين الجديدين
```
مشروعك/
├── login.html          ← أضف هذا الملف (الجديد)
└── js/
    └── auth.js         ← أضف هذا الملف (الجديد)
```

### الخطوة 2: عدّل search.html
افتح ملف `search.html` وأضف هذا السطر **قبل سطر `</head>`**:

```html
    <link rel="stylesheet" href="css/search.css">
    <script src="js/auth.js"></script>        ← أضف هذا السطر
</head>
```

### الخطوة 3: عدّل dashboard.html
افتح ملف `dashboard.html` وأضف هذا السطر **قبل سطر `</head>`**:

```html
    <link rel="stylesheet" href="css/dashboard.css">
    <script src="js/auth.js"></script>        ← أضف هذا السطر
</head>
```

---

## ✅ انتهى!

الآن جميع صفحات الوظائف محمية ✅

---

## 🧪 الاختبار:

### 1. اختبر الحماية:
```
افتح: search.html (مباشرة)
✅ يجب أن يحولك تلقائياً لـ login.html
```

### 2. اختبر تسجيل الدخول:
```
البريد: demo@jobhub.com
كلمة المرور: demo123
✅ يجب أن يحولك لـ search.html
```

---

## 🎯 لحماية صفحة جديدة:

افتح ملف `js/auth.js` واذهب للسطر 68 وأضف اسم الصفحة:

```javascript
const protectedPages = [
    'search.html',
    'dashboard.html',
    'jobs.html',
    'your-new-page.html'    ← أضف هنا
];
```

---

## 🔧 التخصيص:

### لتغيير رسالة "يجب تسجيل الدخول":
لا توجد رسالة! النظام يحوّل مباشرة لصفحة التسجيل (أسرع وأفضل)

### لتغيير الصفحة بعد تسجيل الدخول:
في `login.html` السطر 189، غيّر:
```javascript
window.location.href = intended || 'search.html';  ← غير هنا
```

---

## 📱 بيانات التجربة:

```
البريد: demo@jobhub.com
كلمة المرور: demo123
```

---

## ❓ مشاكل شائعة:

### المشكلة: "الصفحة لا تُحمى"
**الحل:** تأكد أنك أضفت `<script src="js/auth.js"></script>` في الصفحة

### المشكلة: "يظهر خطأ 404"
**الحل:** تأكد أن ملف `js/auth.js` موجود في المسار الصحيح

### المشكلة: "يسجل خروج تلقائي"
**الحل:** هذا طبيعي إذا أغلقت المتصفح (localStorage يُمسح)

---

## 🎉 الخلاصة:

✅ **ملفين جديدين فقط**  
✅ **سطرين تعديل في search.html و dashboard.html**  
✅ **كل الصفحات محمية تلقائياً**  

---

## 📞 هل تحتاج مساعدة؟

راجع الكود - كل شيء موثق ومشروح بالعربي! 😊

---

**JobHub KSA v1.1** • نظام الحماية الكامل ✅
