# 📋 دليل نظام الإصدارات - Version Management Guide

## 🎯 نظرة عامة

تم إضافة نظام إدارة إصدارات متكامل لـ **JobHub KSA / LinkedOFF** لضمان تتبع دقيق للإصدارات والتحديثات.

---

## 📍 أماكن عرض الإصدار

### 1️⃣ Version Badge (الشارة الثابتة)
- **الموقع:** أسفل يسار الشاشة (ثابت في كل الصفحات)
- **الشكل:** `v1.2.0`
- **المميزات:**
  - ظاهر دائماً في كل الصفحات
  - يتحرك عند التمرير فوقه
  - لون أزرق بـ gradient LinkedIn

### 2️⃣ Footer Version (تذييل الصفحة)
- **الموقع:** أسفل الصفحة في الـ Footer
- **الشكل:** `v1.2.0 - 25 ديسمبر 2024`
- **المميزات:**
  - يعرض رقم الإصدار + تاريخ الإصدار
  - موحد في جميع الصفحات

### 3️⃣ Console Log
- **الموقع:** Console المتصفح (F12)
- **الشكل:** `LinkedOFF KSA v1.2.0 - 25 ديسمبر 2024`
- **المميزات:**
  - يساعد المطورين في التتبع
  - يظهر تلقائياً عند تحميل أي صفحة

---

## 🔧 كيفية تحديث الإصدار

### الطريقة السهلة (مكان واحد فقط):

**افتح ملف:** `/js/config.js`

**غيّر هذه الأسطر فقط:**

```javascript
const APP_CONFIG = {
    // Version Info
    version: '1.2.0',           // 👈 غيّر هنا: رقم الإصدار
    releaseDate: '2024-12-25',  // 👈 غيّر هنا: تاريخ الإصدار
    buildNumber: '20241225',    // 👈 غيّر هنا: رقم البناء
    
    // ... باقي الكود
};
```

**مثال على تحديث:**
```javascript
version: '1.3.0',           // من 1.2.0 إلى 1.3.0
releaseDate: '2024-12-26',  // التاريخ الجديد
buildNumber: '20241226',    // البناء الجديد
```

**وبس! 🎉** الإصدار هيتحدّث تلقائياً في:
- ✅ Version Badge
- ✅ Footer في كل الصفحات
- ✅ Console Log
- ✅ Copyright Text

---

## 📝 قواعد ترقيم الإصدارات

نستخدم نظام **Semantic Versioning** (SemVer):

### صيغة الإصدار: `MAJOR.MINOR.PATCH`

**مثال:** `v1.2.3`

- **1** = MAJOR (إصدار رئيسي)
- **2** = MINOR (ميزات جديدة)
- **3** = PATCH (إصلاحات)

### متى تزيد كل رقم:

#### 1️⃣ MAJOR (الرقم الأول):
```
1.0.0 → 2.0.0
```
**استخدم عند:**
- تغييرات جذرية في التصميم
- إزالة ميزات قديمة
- تغيير كامل في طريقة العمل

**مثال:**
- تحويل الموقع من Multi-Page لـ SPA
- إعادة تصميم كاملة

#### 2️⃣ MINOR (الرقم الثاني):
```
1.0.0 → 1.1.0
```
**استخدم عند:**
- إضافة ميزات جديدة
- تحسينات كبيرة
- صفحات جديدة

**مثال:**
- إضافة صفحة Jobs Feed
- إضافة نظام Subscription
- إضافة Infinite Scroll

#### 3️⃣ PATCH (الرقم الثالث):
```
1.0.0 → 1.0.1
```
**استخدم عند:**
- إصلاح أخطاء (Bug Fixes)
- تحسينات بسيطة
- تعديلات CSS صغيرة

**مثال:**
- إصلاح خطأ في Login
- تحسين ألوان
- إصلاح Typo

---

## 📅 مثال على سجل الإصدارات

```
v1.0.0 - 2024-12-01
- أول إصدار من الموقع
- صفحات: Index, Login, Dashboard, Search

v1.1.0 - 2024-12-10
- إضافة Jobs Feed
- إضافة Subscription Modal
- تحسين Dashboard

v1.2.0 - 2024-12-25
- إضافة Version Management System
- تحسين Footer
- إضافة Console Logging

v1.2.1 - 2024-12-26
- إصلاح خطأ في Login Button
- تحسين ألوان Version Badge
```

---

## 🎨 التخصيص المتقدم

### تغيير شكل Version Badge:

افتح `/css/styles.css` وابحث عن:

```css
.version-badge {
    position: fixed;
    bottom: 20px;        /* 👈 المسافة من الأسفل */
    left: 20px;          /* 👈 المسافة من اليسار */
    background: linear-gradient(135deg, #0A66C2, #004182);
    color: white;
    padding: 8px 16px;
    border-radius: 20px;
    font-size: 13px;
    /* ... */
}
```

### تغيير موقع Badge:

**أسفل يمين بدلاً من يسار:**
```css
.version-badge {
    bottom: 20px;
    right: 20px;  /* غيّر من left لـ right */
}
```

**أعلى يسار:**
```css
.version-badge {
    top: 20px;    /* غيّر من bottom لـ top */
    left: 20px;
}
```

### إخفاء Version Badge:

في `/js/config.js`:
```javascript
showVersionBadge: false,  // غيّر من true لـ false
```

ثم في كل صفحة HTML، احذف:
```html
<div class="version-badge" id="versionBadge">v1.2.0</div>
```

---

## 🔍 الدوال المتاحة في config.js

### 1️⃣ `getVersionString()`
```javascript
APP_CONFIG.getVersionString()
// Output: "v1.2.0 - 25 ديسمبر 2024"
```

### 2️⃣ `getVersionShort()`
```javascript
APP_CONFIG.getVersionShort()
// Output: "v1.2.0"
```

### 3️⃣ `getVersionArabic()`
```javascript
APP_CONFIG.getVersionArabic()
// Output: "الإصدار 1.2.0"
```

### 4️⃣ `getFooterText()`
```javascript
APP_CONFIG.getFooterText()
// Output: "© 2024 LinkedOFF KSA by AceAlBastoni. جميع الحقوق محفوظة."
```

---

## 📂 الملفات المتأثرة

عند تحديث الإصدار، تأكد من وجود config.js في هذه الصفحات:

| الملف | config.js موجود؟ |
|-------|------------------|
| index.html | ✅ نعم |
| dashboard.html | ✅ نعم |
| search.html | ✅ نعم |
| login.html | ✅ نعم |

---

## ✅ Checklist قبل الإصدار

قبل رفع إصدار جديد:

- [ ] حدّث `version` في `/js/config.js`
- [ ] حدّث `releaseDate` في `/js/config.js`
- [ ] حدّث `buildNumber` في `/js/config.js`
- [ ] اختبر كل الصفحات (index, dashboard, search, login)
- [ ] تأكد من ظهور Version Badge في كل صفحة
- [ ] تأكد من ظهور Version في Footer
- [ ] افتح Console وتأكد من ظهور رسالة الإصدار
- [ ] اكتب سجل التغييرات في README أو CHANGELOG

---

## 🆘 استكشاف الأخطاء

### المشكلة: Version Badge لا يظهر
**الحل:**
1. تأكد من وجود `<script src="js/config.js"></script>` في الصفحة
2. تأكد من وجود `<div class="version-badge" id="versionBadge">v1.2.0</div>`
3. افتح Console وابحث عن أخطاء JavaScript

### المشكلة: Version في Footer لا يتحدث
**الحل:**
1. تأكد من وجود `id="footerVersion"` في الـ p tag
2. تأكد من تحميل `config.js` قبل نهاية الـ body

### المشكلة: التاريخ بالإنجليزي
**الحل:**
- التواريخ في `config.js` تُحوَّل تلقائياً للعربية
- تأكد من صحة التاريخ في `releaseDate`

---

## 📞 الدعم

**المطور:** AceAlBastoni  
**WhatsApp:** 0566114725  
**STC Pay:** 0566114725  

---

**آخر تحديث:** 25 ديسمبر 2024  
**الإصدار الحالي:** v1.2.0
